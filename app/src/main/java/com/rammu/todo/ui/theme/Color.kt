package com.rammu.todo.ui.theme


import androidx.compose.ui.graphics.Color

// --- Brand Orange ---
val Orange500 = Color(0xFFFF9800) // A vibrant, standard orange
val Orange700 = Color(0xFFFB8C00) // A slightly darker orange for contrast/darker variants
val Orange300 = Color(0xFFFFA726) // A lighter orange for highlights or light themes

// --- Primary Black/Dark Grays ---
val Black800 = Color(0xFF212121) // A deep, but not pure black, good for backgrounds
val Black900 = Color(0xFF000000) // Pure black, sparingly used for text on light backgrounds
val DarkGray700 = Color(0xFF424242) // A medium-dark gray for surfaces in dark mode
val DarkGray500 = Color(0xFF616161) // A lighter dark gray for less prominent elements

// --- White/Light Grays ---
val White = Color(0xFFFFFFFF)
val LightGray100 = Color(0xFFF5F5F5) // Very light gray for backgrounds in light mode
val LightGray300 = Color(0xFFE0E0E0) // A medium light gray for surfaces in light mode

// --- Accent/Complementary (optional, but good for visual interest) ---
val Teal200 = Color(0xFF03DAC5) // A common Material Teal, can complement orange well
val Teal700 = Color(0xFF018786)

// --- Error Colors ---
val RedErrorLight = Color(0xFFB00020) // Standard Material error red for light theme
val RedErrorDark = Color(0xFFCF6679) // Lighter red for error on dark backgrounds